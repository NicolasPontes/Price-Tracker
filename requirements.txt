# --- Web / API ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.9,<1.0   # necessário para o form de login (OAuth2PasswordRequestForm)

# --- Banco de dados ---
sqlalchemy>=2.0,<3.0
psycopg2-binary>=2.9,<3.0       # driver de conexão com PostgreSQL

# --- Scraping / integração com marketplaces ---
requests>=2.32,<3.0

# --- Agendamento ---
apscheduler>=3.10,<4.0

# --- Autenticação ---
python-jose[cryptography]>=3.3,<4.0   # geração/validação de JWT
passlib[bcrypt]>=1.7,<2.0             # hash de senha
bcrypt==4.0.1                         # versão fixa: versões mais novas quebram compatibilidade com o passlib

# --- Configuração / validação ---
python-dotenv>=1.0,<2.0
pydantic[email]>=2.9,<3.0             # validação de e-mail nos schemas de usuário